//Function <приема, връща>
// Consumer <приема> -> void
// Supplier <връща>
// Predicate <приема> -> връща true/false
// BiFunction <приема1, приема2, връща>

Comparator -> двойки числа
//1 -> п < в
//-1 -> п > в
//0 -> п == в

Sort() -> взима всички двойки числа -> Comparator
// ако 1 -> двете числа не променят мястото си
// ако -1 -> двете числа сменят местата си
// ако 0 -> двете числа не променят мястото си
